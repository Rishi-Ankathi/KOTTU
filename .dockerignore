# The image only needs api/ and models/. Everything else is excluded so the
# build context stays small and fast to send to the Docker daemon.

.git/
web/
src/
scripts/
data/
output/
docs/
tests/
train.py
*.md
.venv/
venv/
**/__pycache__/
**/*.pyc
