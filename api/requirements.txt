# KOTTU API - runtime deps only.
#
# The model runs through the standalone TF-Lite runtime, not full TensorFlow:
# models/kottu_model.tflite and models/serving_params.json are produced on a
# training machine by scripts/export_serving.py. Nothing here imports
# tensorflow, scikit-learn, or joblib.

fastapi
uvicorn[standard]

tflite-runtime==2.14.0 ; sys_platform == "linux"
numpy==1.24.3
